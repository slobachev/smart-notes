services:
  postgres:
    image: postgres:16-alpine
    container_name: fs-learn-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fs_learn
    ports:
      - "9999:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
